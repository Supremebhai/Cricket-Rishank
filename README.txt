v8 - Yellow DP glow + animated blue bottom bar
-----------------------------------------------
Changes in this version:
- Yellow animated glow added behind DP (avatar-glow).
  * Subtle pulsing via keyframes (glowPulse).
- Bottom bar updated:
  * Blue three-color gradient (#0044ff -> #0088ff -> #00bbff).
  * Gradient moves horizontally (background-size:300%) using bottomMove.
  * Pop/pulse vertical animation using bottomPop for lively effect.
- Fog layers remain as moving background layers for depth.

How to preview:
1. Unzip the package.
2. Open index.html in a browser (local web server recommended for best performance).
3. Files:
   - index.html
   - style.css
   - images/bg.jpg
   - images/dp.jpg

Want further tweaks? Tell me:
- glow strength (softer/harder), glow color (darker yellow / orange), or one-time animation
- bottom animation speed, height, or color tone
- make package fully offline (replace external fog image)
